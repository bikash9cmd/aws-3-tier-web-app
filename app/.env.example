# Copy to .env for local development
# Never commit .env to Git

PORT=3000
NODE_ENV=development

# RDS connection (from terraform output rds_endpoint)
DB_HOST=localhost
DB_USER=admin
DB_PASSWORD=your_password_here
DB_NAME=appdb

S3_BUCKET=your-bucket-name
